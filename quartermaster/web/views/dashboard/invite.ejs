<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Invite Bot - <%= guild.name %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="dashboard-container">
        <div class="container">
            <div class="empty-state">
                <h1>Bot Not Added</h1>
                <p>The bot needs to be added to <%= guild.name %> before you can configure it.</p>
                <a href="<%= inviteUrl %>" class="btn btn-primary btn-large" target="_blank">Invite Bot to Server</a>
                <a href="/dashboard" class="btn btn-secondary">Back to Dashboard</a>
            </div>
        </div>
    </div>

    <%- include('../partials/footer') %>
</body>
</html>
